{
  "name": "surething-clone",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "db:generate": "turbo db:generate --filter=@surething/server",
    "db:migrate": "turbo db:migrate --filter=@surething/server",
    "db:studio": "turbo db:studio --filter=@surething/server",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0"
  }
}